#List S3 Bucket

import boto3

bucket = 'test_bucket'

prefix = 'mynewfolders/'  

client = boto3.client('s3')

output = client.list_objects(Bucket=bucket, Prefix=prefix, Delimiter='/')

for i in output.get('CommonPrefixes'):
    print 'objects : ', i.get('Prefix')

